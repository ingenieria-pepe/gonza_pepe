# Secretos y datos personales: NUNCA al repo (es publico). Sacados del indice el
# 04/09/2026; el token que estaba en el commit 313d989 se da por quemado.
# (gitignore no admite comentarios al final de la linea: van arriba de cada patron)
# token Whapi + telefonos destino:
config/whatsapp.json
# la salida del pipeline lista los telefonos destino:
last_run.log

# Estado DEL SERVIDOR: memoria anti-spam de WhatsApp, caches y logs. Sacados del
# indice el 04/09/2026: si viajaran por git, cualquier commit de la laptop que los
# tocara trabaria la sincronizacion. correr_servidor.ps1 los respalda y restaura
# en cada corrida (lista $preservar).
fuentes/state_alertas.json
fuentes/state_whatsapp.json
fuentes/precios_py_cache.json
fuentes/indexmundi_cache.json
fuentes/clima_archive_cache/
alertas.log
# planillas que alguien edito en el servidor y el espejo iba a pisar:
respaldos_servidor/

# Respaldos y zips: no van al repo (el de 16 MB se saco del indice el 04/09/2026)
*.zip
*.bak
*.pre-splice.bak
